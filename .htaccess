# 1. Enable Rewrite Engine
RewriteEngine On
RewriteBase /clientfilingindiademo/

# 2. Check if a clean URL corresponds to an existing .php file
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME}.php -f
RewriteRule ^(.*)$ $1.php [L]
